# aspyn

A minimal CLI for stateful shell pipelines.

## Conventions

- **Project under active delevlopment.** Breaking changes are expected by end users. No extra effort needs to be made to avoid them.
- **No barrel files.** Never create `index.ts` re-export files. Import directly from the file that defines what you need.
